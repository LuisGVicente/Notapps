<div class="col-md-4 mx-auto">
    <div class="card">
        <div class="card-header">
            <h3>Edit Note</h3>
        </div>
        <div class="card-body">
            {{!-- Como sólo pueden mandarse métodos POST y GET en un formulario, tenemos que usar el middleware de src/index.js (punto 9 )para poder usar el método PUT. Para eso usamos _method --}}
            <form action="/notes/edit-note/{{note._id}}?_method=PUT" method="POST">
            <input type="hidden" name="_method" value="PUT">
                <div class="form-group">
                    <input type="text" name="title" class="form-control" placeholder="Title" value="{{note.title}}">
                </div>
                <div class="form-group">
                    <textarea  name="description" class="form-control">{{note.description}}</textarea>
                </div>
                <div class="form-group">
                    <button class="btn btn-primary brn-block" type="submit">Save</button>
                </div>
                
            </form>
        </div>
    </div>
</div>