<div class="row">
    {{#each notes}}
    <div class="col-md-3">
        <div class="card">
            <div class="card-body">
                <h4 class="card-title d-flex justify-content-between align-items-center">
                    {{title}} <a href="/notes/edit/{{_id}}"><i class="fas fa-edit"></i></a>
                </h4>
                <p>{{description}}</p>
                {{!-- Como sólo pueden mandarse métodos POST y GET en un formulario, tenemos que usar el middleware de src/index.js (punto 9 )para poder usar el método DELETE. Para eso usamos _method --}}
                <form action="/notes/delete/{{_id}}?_method=DELETE" method="POST">
                    <input type="hidden" value="DELETE" name="_method">
                    <button class="btn btn-danger btn-block btn-sm" type="submit">Delete</button>
                </form>
            </div>
        </div>
    </div>
    {{else}}
    <div class="card mx-auto">
        <div class="card-body">
            <p class="lead">You have no notes yet</p>
            <a href="/notes/add" class="btn btn-success btn-block">Create one!</a>
        </div>
    </div>
    {{/each}}
</div>